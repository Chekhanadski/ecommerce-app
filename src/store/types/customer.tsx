export interface CustomerData {
  firstName: string;
  lastName: string;
  email: string;
  dateOfBirth: string;
}

export interface CustomerAddress {
  city: string;
  country: string;
  postalCode: string;
  streetName: string;
  streetNumber: string;
}
